apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airflow-data
  namespace: bcwat
spec:
  accessModes: ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: airflow
  volumeMode: Filesystem
