{{- if false }}
# Commented out as we manually created secret - will revisit in the future
{{- if and .Values.crunchy.enabled .Values.crunchy.pgBackRest.s3.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: s3-secret-conf
type: Opaque
stringData:
  s3.conf: |-
    {{ include "crunchy.s3" . | nindent 4 }}
{{- end }}
{{- end }}
