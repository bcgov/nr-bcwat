apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: restore-to-db
spec:
  podSelector:
    matchLabels:
      postgres-operator.crunchydata.com/cluster: bcwat-test-crunchy
  ingress:
    - ports:
        - protocol: TCP
          port: 5432
      from:
        - podSelector:
            matchLabels:
              role: restore-db
  policyTypes:
    - Ingress
