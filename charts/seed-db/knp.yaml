apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-seed-script-to-db
spec:
  podSelector:
    matchLabels:
      postgres-operator.crunchydata.com/cluster: bcwat-dev-crunchy
  ingress:
    - ports:
        - protocol: TCP
          port: 5432
      from:
        - podSelector:
            matchLabels:
              role: seed-db
  policyTypes:
    - Ingress
